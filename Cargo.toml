[package]
name = "zellaude"
version = "0.5.9"
edition = "2021"

[dependencies]
# Non-caret so a future Zellij minor resolves without a manifest bump;
# Cargo.lock still pins the tested version until `cargo update`.
zellij-tile = ">=0.44.3"
zellij-utils = ">=0.44.3"
# In the dependency tree via zellij-utils, which neither re-exports it nor
# links it — first real use costs ~153 KiB of wasm.
kdl = "4.7"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
unicode-width = "0.1.8"

[profile.release]
opt-level = "s"
lto = true
